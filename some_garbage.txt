passwords: web2021$AK

matches(/(?=.*?[^[α-ωΑ-Ω]])/,"Email addresses should only contain Latin characters")


JWT token secret: Gh5HQXhGBWs24fpAIRmAbn0TELM4

    <div>
      {role === "visitor" && <User />}
      {role === "admin" && <Admin />}
    </div>

      <h1>{ip}</h1>
      <h4>{harFile}</h4>